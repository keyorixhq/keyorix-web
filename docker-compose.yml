version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    depends_on:
      - backend
    networks:
      - keyorix-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  backend:
    image: keyorix-backend:latest
    ports:
      - "8080:8080"
    environment:
      - ENV=production
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=${DB_NAME:?set DB_NAME in .env, see .env.example}
      - DB_USER=${DB_USER:?set DB_USER in .env, see .env.example}
      - DB_PASSWORD=${DB_PASSWORD:?set DB_PASSWORD in .env, see .env.example}
    depends_on:
      - postgres
    networks:
      - keyorix-network
    restart: unless-stopped

  postgres:
    image: postgres:15-alpine
    environment:
      - POSTGRES_DB=${DB_NAME:?set DB_NAME in .env, see .env.example}
      - POSTGRES_USER=${DB_USER:?set DB_USER in .env, see .env.example}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in .env, see .env.example}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - keyorix-network
    restart: unless-stopped

volumes:
  postgres_data:

networks:
  keyorix-network:
    driver: bridge