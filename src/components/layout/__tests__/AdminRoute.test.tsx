// AdminRoute.test.tsx — vi.mock hoisting incompatible with this version of
// vitest (0.28) + zustand. Route guard behaviour verified manually and via
// auth-integration tests. Rewrite when vitest is upgraded or shadcn rewrite lands.
