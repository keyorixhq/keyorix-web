# Encryption

Purpose:
- secure storage of secrets

Requirements:
- secrets must never be stored in plaintext
- encryption must be consistent

Rules:
- do not change encryption logic
- do not weaken security
- do not expose secrets

Assumptions:
- encryption/decryption handled centrally