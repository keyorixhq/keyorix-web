# MCP

Purpose:
- interface between AI and Keyorix
- enable commands like "show secrets"

Responsibilities:
- parse user intent
- call backend APIs
- format responses

Key features:
- NLP-based commands
- structured outputs

Rules:
- do not change command format
- do not invent new APIs
- keep responses deterministic
